{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "This is node server for Let's chat application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js"
  },
  "author": "Shilpa",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.12",
    "socket.io": "^2.3.0"
  }
}
